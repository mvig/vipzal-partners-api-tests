{
  "name": "test-api-partners",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-dev": "newman run tests/test-api-partners.postman_collection.json -e tests/dev.vipzal.dev.vip-zal.ru.postman_environment.json --reporters cli,junit,html --reporter-junit-export newman.xml --reporter-html-export newman.html"
  },
  "author": "Igor Mets",
  "dependencies": {
    "newman": "^3.9.4"
  }
}
